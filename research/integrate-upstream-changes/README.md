# Upstream integration research

The design rationale remains in `00-problem-and-design.md`. The operational
procedure, ledger reference, and templates moved to
`ops/upstream/skills/upstream-review/` so the upstream capability owns the only
copy.
