# Frontend module fixture

This disposable module proves public-entrypoint composition, lazy panel
loading, and namespaced Tauri invocation. The default product profile does not
import it. The explicit fixture smoke profile is its only frontend consumer.
