schema_version: 1
id: agents.wakeup
enabled: true
cron: "*/5 * * * * Europe/Warsaw"
target:
  kind: channel
  id: agents.wakeup
message:
  type: fixture.agent-wakeup
  version: 1
  payload:
    reason: scheduled
