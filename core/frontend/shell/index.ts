// The app shell: the one place that composes several capabilities into a
// single screen. Not a capability itself — see README.md, rule 4.
//
// Only src/main.tsx imports this.
export { default as App } from "./App.tsx";
