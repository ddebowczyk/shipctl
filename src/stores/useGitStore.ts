import { ENABLED_GIT_FRONTEND } from "../core/modules/enabledModules";

/** Temporary import adapter; removed with the Git visual-surface extraction. */
export const useGitStore = ENABLED_GIT_FRONTEND.useGitStore;
