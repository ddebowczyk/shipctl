[package]
name = "tauri-plugin-shep-fixture"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
publish = false
links = "tauri-plugin-shep-fixture"

[lib]
name = "shep_module_fixture"
path = "src/lib.rs"

[dependencies]
shep-module-api = { path = "../../api/backend" }
tauri = { version = "2.11.5", default-features = false }

[build-dependencies]
tauri-plugin = { version = "2.5.4", features = ["build"] }

[dev-dependencies]
tauri = { version = "2.11.5", default-features = false, features = ["test"] }
