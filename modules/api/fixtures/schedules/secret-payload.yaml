schema_version: 1
id: agents.secret
enabled: true
cron: "*/5 * * * * Europe/Warsaw"
target:
  kind: channel
  id: agents.secret
message:
  type: fixture.agent-wakeup
  version: 1
  payload:
    apiToken: fixture-secret
