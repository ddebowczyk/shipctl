schema_version: 1
id: agents.canonical
enabled: true
cron: "*/5 * * * * Europe/Warsaw"
target:
  kind: channel
  id: agents.canonical
message:
  type: fixture.agent-wakeup
  version: 1
  payload:
    metadata:
      beta:
        dog: 2
        cat: 1
      alpha: first
    reason: canonical
