---
schema_version: 1
id: module-control
provides: module-control
description: Verify module-control contracts and public boundaries.
status: experimental
requires:
  tools: [bash, cargo, just, node, pnpm]
  capabilities: []
owns: [examples/rapid-terminal-probe/**, ops/module-control/**]
reads:
  - Cargo.toml
  - Cargo.lock
  - cli/**
  - core/**
  - modules/**
  - ops/build/**
  - package.json
  - pnpm-lock.yaml
  - src/**
  - src-tauri/**
generates:
  - target/**
commands:
  - name: offline-cli
    summary: Verify offline runtime artifact admission through the public CLI.
    lane: slow
  - name: integration
    summary: Verify the live module-control boundary.
    lane: slow
    args: [args]
skills: []
