types:
    pnpm --dir ../.. exec tsc --noEmit

types-all: types
    pnpm --dir ../.. exec tsc --noEmit -p ops/modularity/fixtures/panel-host/tsconfig.json
    pnpm --dir ../.. exec tsc --noEmit -p ops/modularity/fixtures/module-fixture/tsconfig.json

fmt:
    cargo fmt --all -- --check

clippy-soft:
    node bin/clippy-soft.mjs

schemas:
    node bin/check-schemas.mjs

manifests:
    node bin/check-manifests.mjs

version:
    just --justfile ../version/justfile check

instance-paths:
    node bin/check-instance-paths.mjs

# Area 05's negative proof: the dev-only scenario harness must not ship.
release-bundle:
    node bin/check-release-bundle.mjs

all:
    node bin/run-aggregate.mjs check
