# Operations attic

Nothing here is supported or included in repository command lanes.

| File | Purpose | Last used | Current status |
| --- | --- | --- | --- |
| `bin/probe-usage.sh` | Exploratory probes for provider usage sources. | 2026-07-06 | Help mode works; provider and live probes are not reverified. |
| `bin/import-live-agent-recovery.mjs` | One-time import of a machine-specific assistant recovery snapshot. | 2026-08-05 | Help mode works; apply mode is intentionally not reverified. |
| `bin/update_model_pricing.py` | One-off refresh of the Usage module pricing snapshot from models.dev. | 2026-08-07 | Fetch and output generation work; the verification output was discarded. |
